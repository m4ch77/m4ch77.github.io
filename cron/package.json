{
  "name": "m4ch77-sync-cron",
  "version": "1.0.0",
  "private": true,
  "description": "Notion 동기화를 5분마다 깨우는 Cloudflare Worker. GitHub 의 schedule 이 지켜지지 않아서 시계만 옮겨온 것입니다.",
  "//": "wrangler 는 메이저 버전마다 명령 문법이 바뀝니다. 그래서 고정해 둡니다.",
  "//check": "올리기 전에 구문을 먼저 봅니다. 블록 주석 안에 cron 식을 적었다가 주석이 조기에 닫혀 배포가 실패한 적이 있습니다.",
  "scripts": {
    "check": "node --check worker.js && wrangler deploy --dry-run --outdir=.wrangler/dry",
    "secret": "wrangler secret put GH_TOKEN",
    "deploy": "npm run check && wrangler deploy",
    "tail": "wrangler tail",
    "try": "wrangler dev --test-scheduled"
  },
  "devDependencies": {
    "wrangler": "4.119.0"
  }
}

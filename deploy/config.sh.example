# deploy/config.sh 로 복사해서 값을 채우세요.
# (config.sh 는 계정 정보가 들어가니 저장소에 올리지 않는 편이 좋습니다)

# 스택 출력 BucketName
export SITE_BUCKET="m4ch77-site-000000000000"

# 스택 출력 DistributionId
export SITE_DIST_ID="E000000000000A"

# 여러 계정을 쓸 때만
# export AWS_PROFILE="m4ch77"
export AWS_REGION="ap-northeast-2"

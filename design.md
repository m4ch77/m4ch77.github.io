# m4ch77 — design system

여러 사이트(`m4ch77.com`, `resume.m4ch77.com`, `blog.m4ch77.com`)가 같은 얼굴을 갖게 하려고 정리한 문서입니다.

실제로 통일을 **강제하는 것은 `theme.css`** 이고, 이 문서는 그 값을 **왜 그렇게 정했는지와 어떻게 쓰는지**를 적습니다.
문서만 있으면 반드시 어긋나므로, 새 사이트는 값을 베끼지 말고 `theme.css` 를 그대로 불러 쓰세요.

```html
<link rel="stylesheet" href="theme.css">   <!-- 공용 토큰 -->
<link rel="stylesheet" href="styles.css">  <!-- 그 사이트만의 조립 -->
```

---

## 1. 원칙

1. **색은 거들 뿐입니다.** 화면의 99%는 배경과 글자, 1%가 강조색입니다. 강조색이 대표색처럼 보이면 과한 것입니다.
2. **서피스는 두 단계뿐입니다.** 섹션을 `1 / 2` 로 번갈아 두어 리듬을 만듭니다. 3단계를 만들지 않습니다.
3. **밤에 편한 것이 먼저입니다.** 다크 모드에서 흰 면을 쓰지 않습니다. 두 서피스 모두 어둡습니다.
4. **한 섹션은 한 화면입니다.** 스크롤 한 번에 한 장면이 넘어갑니다.
5. **모션은 하나의 곡선을 씁니다.** `--ease` 를 쓰고 길이만 바꿉니다. 등장은 자리마다 다르게 하되 곡선은 건드리지 않습니다.
6. **런타임 의존성은 0.** 빌드 시점 도구는 씁니다. 마크다운·수식·코드 강조는 빌드가 끝내고 방문자에게는 완성된 HTML·CSS 만 갑니다. 브라우저가 파서를 내려받는 일은 없습니다.

---

## 2. 색

값은 `theme.css` 에 있습니다. 여기서는 **어디에 쓰는지**만 정합니다.

| 토큰 | 쓰는 곳 |
| --- | --- |
| `--bg` | 섹션 배경. `[data-surface]` 가 자동으로 정합니다. |
| `--fg` | 본문 글자, 제목 |
| `--dim` | 보조 설명, 목록 본문 |
| `--faint` | 메타(날짜·번호·힌트), 비활성 |
| `--line` | 기본 구분선, 카드 테두리 |
| `--line2` | 강조된 테두리, 버튼 외곽 |
| `--card` | 카드·패널 배경 (배경 위에 아주 얇게 얹는 층) |
| `--accent` | 아래 "강조색을 쓰는 자리"만 |
| `--accent-soft` | 강조색 배경(코드 조각, 잔광) |
| `--on-accent` | 강조색 위에 올라가는 글자색 |

### 강조색을 쓰는 자리 (이 목록 밖에서는 쓰지 않습니다)

- 링크, 그리고 링크임을 알리는 밑줄
- 활성 상태: 내비게이션 현재 위치, 캐러셀 현재 점, 선택된 항목
- 진행 표시: 스크롤 진행 바, 로더 바
- 터미널 프롬프트와 키워드
- 브랜드 마크(헤더의 작은 사각형), 커서 강조 대신은 아님
- 숫자·기호 한 글자짜리 포인트 (`+`, 연도)

### 쓰지 않는 자리

- 큰 면적을 채우는 배경(띠, 배너)
- 커서 — 커서는 색 대신 `mix-blend-mode: difference` 반전으로 그립니다
- 본문 글자

### 두 테마

`data-theme="dark" | "light"` 를 `<html>` 에 둡니다. 테마가 글자색과 선을 정하고, 서피스는 배경 단계만 정합니다.

```
dark   : bg-1 #0a0b0d   bg-2 #14161c   fg #e6e8ec   accent #7aa2ff
light  : bg-1 #ece9e2   bg-2 #e0dcd3   fg #191815   accent #2a52cc
```

두 서피스의 차이는 크지 않습니다. 경계는 배경 차이 + 얇은 선 + 여백으로 만듭니다.

밝은 테마는 순백을 쓰지 않습니다. 눈이 시리고 사진·커버 이미지가 떠 보입니다.
따뜻한 상아색으로 내리고 글자도 파란 검정이 아니라 따뜻한 검정을 씁니다.

---

## 3. 타입

한 가족만 씁니다. 라틴과 한글의 골격이 달라 보이는 것을 막기 위한 선택입니다.

- 본문 · 제목: **IBM Plex Sans KR** (`--font-ui`)
- 코드 · 메타 · 브랜드: **IBM Plex Mono** (`--font-mono`)
- 세리프는 쓰지 않습니다.

굵기로 대비를 만듭니다.

| 용도 | 크기 | 굵기 |
| --- | --- | --- |
| 히어로 | `clamp(2.4rem, 1.5rem + 4.4vw, 5rem)` | 300 |
| 닉네임 (히어로 안) | 상속 | 600, mono |
| 섹션 제목 | `clamp(2rem, 1.4rem + 2.8vw, 3.6rem)` | 600 |
| 제목 속 강조 `.thin` | 상속 | 600 (같은 굵기) + 강조색 아웃라인 |
| 소제목 (제목 아래 한글) | `0.92rem` | 400, `--dim` |
| 본문 | `clamp(1rem, …, 1.05rem)` | 400 |
| 메타 · 키커 | `0.6 ~ 0.72rem`, mono, `letter-spacing: 0.12em`, 대문자 | 400 |

### 섹션 제목은 영어, 설명은 한글

큰 제목은 짧은 영어 문장(`What I Wrote`, `What I Learned`)으로 두고, 바로 아래 작은 한글로 무엇인지 설명합니다.
큰 글씨에서 한글은 자간·줄간을 계속 손봐야 하는데, 영어는 그대로 두어도 안정적입니다. 뜻은 한글이 담당합니다.

한글 규칙

- `word-break: keep-all` — 단어 중간에서 끊지 않습니다.
- 큰 제목은 `letter-spacing: -0.035em` 정도로 조입니다.
- 단어 마스크 애니메이션에는 `overflow-clip-margin` 을 씁니다. 한글 폰트의 세로 여백이 `line-height` 보다 커서 패딩 없이 자르면 글자 아래가 잘립니다.

---

## 4. 치수 · 간격

- 셸 최대 폭 `--shell: 1280px`, 좌우 여백 `--gutter: clamp(1.25rem, 4.5vw, 4rem)`
- 반지름: 작은 것 `--radius: 4px`, 카드·패널 `--radius-lg: 16px`, 알약은 `99px`
- 섹션 위아래 여백 `clamp(4.5rem, 10vh, 7rem)` — 이 값이어야 한 섹션이 한 화면에 들어갑니다
- 선은 항상 1px. 굵은 구분선을 만들지 않습니다

---

## 5. 모션

- 곡선은 `--ease: cubic-bezier(0.22, 1, 0.36, 1)` 하나
- 길이: `--t-fast 180ms`(색·테두리), `--t 400ms`(형태), `--t-slow 880ms`(등장)
- 등장은 `translateY(20px) + opacity`, 형제 순서대로 65ms 씩 지연
- 스크롤 정렬은 CSS `scroll-snap` 이 아니라 JS로 굴립니다. 스냅은 걸리는 순간이 갑작스럽고 속도를 조절할 수 없습니다. 손을 뗀 뒤 160ms 기다리고, 600~1000ms 동안 `easeInOutCubic` 으로 붙입니다. 다시 손을 대면 즉시 멈춥니다.
- **등장은 자리마다 다르게, 곡선은 하나로.** `[data-reveal]` 의 **값**으로 갈립니다. JS 는 `.is-in` 만 붙이고 나머지는 CSS가 합니다.

| 값 | 결 | 쓰는 곳 |
| --- | --- | --- |
| (없음) | 아래에서 20px | 기본 |
| `rise` | 아래에서 56px, 조금 더 길게 | FAQ |
| `slide` | 왼쪽에서 30px | 블로그 카드 |
| `wipe` | 왼→오로 닦아냄 | 글 목록 행 |
| `draw` | 세로선이 위→아래로 그려짐 | 약력 타임라인 |

  `data-split-dir="x"` 를 더하면 단어·글자가 **왼쪽에서** 들어옵니다.
  TIL 잔디는 열(주)마다 `--i` 를 받아 왼쪽부터 자라 오릅니다.

- **`clip-path` 로 관찰 대상 자신을 가리지 마세요.** 면적이 0이 되면 `IntersectionObserver` 가 "화면에 없다"고 보고합니다. 그러면 `.is-in` 이 영원히 안 붙고, 안 붙으니 계속 가려집니다. 서로를 막습니다. 대상은 두고 **안쪽 자식만** 가려야 합니다. (실제로 이 버그로 글 목록이 전부 안 보인 적이 있습니다.)
- **무언가를 미리 감추면 반드시 풀어주는 길을 두세요.** 관찰이 걸리지 않는 경우를 대비한 시간 제한이 없으면 내용이 영영 안 보입니다. `clip-path` 는 `transform: none` 으로 풀리지 않으므로 `prefers-reduced-motion` 과 인쇄에서 따로 열어야 합니다.
- ~~`data-flow` (스크롤 속도만큼 `skewY`)~~ 는 **없앴습니다.** 기울기가 "고급스럽게" 읽히는 폭이 아주 좁고, 대부분에게는 글자가 출렁이는 렌더링 오류처럼 보였습니다. 매 프레임 `transform` 을 쓰는 비용도 있었습니다.
- 큰 숫자를 배경에 깔아 여백을 채우지 않습니다. 발표 슬라이드처럼 보입니다. 여백은 제목·소제목·간격으로 잡습니다.
- `prefers-reduced-motion` 에서는 전부 끕니다

---

## 6. 컴포넌트 규칙

### 버튼

채움을 별도 레이어(`::before`)로 올리지 않습니다. 테두리와 반지름이 미세하게 어긋나 테두리가 겹쳐 보입니다.
대신 `background-image` 그라디언트의 `background-size` 를 0% → 100% 로 키웁니다.

```css
.btn {
  border: 1px solid transparent;
  background-image: linear-gradient(var(--accent), var(--accent));
  background-repeat: no-repeat;
  background-position: 50% 100%;
  background-size: 100% 0%;
}
.btn:hover { background-size: 100% 100%; color: var(--on-accent); }
```

포커스 링에는 `border-radius` 를 강제하지 않습니다. 강제하면 알약 버튼에 사각 테두리가 생깁니다.

### 제목 속 강조

굵기를 크게 낮추거나(600 → 200) 밑줄을 긋는 방식은 쓰지 않습니다. 두께 차이가 어색하고 밑줄은 링크와 헷갈립니다.
같은 굵기를 유지한 채 **속을 비우고 강조색으로 윤곽만** 그립니다. 히어로·푸터 워드마크와 같은 처리라 사이트 안에서 일관됩니다.

```css
.thin { color: transparent; -webkit-text-stroke: 0.032em var(--accent); }
```

획 두께는 `em` 으로 두어 글자 크기에 따라 함께 커지게 합니다.

### 카드

`1px solid var(--line)` + `--radius-lg` + `var(--card)` 배경. 호버에서 테두리를 `--line2` 로 올리고 `translateY(-4px)`.

### 링크

밑줄로 링크임을 알립니다. `text-underline-offset` 을 넉넉히(3~7px) 두고, 색은 `--line2` 에서 호버 시 `--accent` 로.

### 가로 캐러셀

`scroll-snap-type: x mandatory` + 화살표 + 점 + 진행 바 + 드래그. 끌고 있는 동안에는 스냅을 끕니다(`.is-dragging`).

인디케이터는 **점 하나에 카드 하나**로 두고, 활성 표시는 점을 하나만 칠하는 대신 **지금 보이는 카드들을 감싸는 캡슐**로 그립니다.
카드가 3장씩 보이는데 점 하나만 칠하면 뒤쪽 점들은 절대 활성화되지 않아 구조가 어긋납니다. 캡슐이 늘어나면 마지막 점까지 자연스럽게 닿습니다.

마지막 칸에는 전체 목록으로 넘어가는 "더보기" 카드를 둡니다. 파선 테두리로 콘텐츠 카드와 구분합니다.

### 터미널

첫 화면 오른쪽에 둡니다. 셸 안에 들어오게 하고 사방을 닫습니다. 화면 끝으로 흘려보내면 잘린 것처럼 보입니다.

### 커서

점과 링은 흰색 + `mix-blend-mode: difference` 로 어떤 배경에서도 보이게 합니다. 상태는 기본, 강조(`is-hot`), 카드 테두리 흡착, 입력(`is-text`), 누름(`is-down`).

성능 주의점 두 개.

- `mix-blend-mode` 는 매 프레임 배경을 다시 합성합니다. **면적이 큰 요소에는 걸지 않습니다.** 카드에 달라붙는 테두리는 blend 대신 `data-surface` 를 따라가는 색을 씁니다.
- 달라붙는 테두리는 위치를 매 프레임 다시 재고 **트랜지션을 걸지 않습니다.** 걸면 카드가 가로로 흐를 때 테두리가 따라오다 어긋나 보입니다. 내부 스크롤러를 잡으려면 `scroll` 을 캡처 단계로 들어야 합니다.
- 드래그하는 동안에는 테두리를 감춥니다.

### 스크롤에 반응하는 모션의 비용

`data-flow` 같은 효과는 매 프레임 `style.transform` 을 쓰기 쉽습니다. 값이 같으면 쓰지 않고, 멈추면 `requestAnimationFrame` 루프 자체를 멈춰야 합니다. 같은 값을 계속 쓰는 것만으로도 포인터가 느려집니다.

### 언어

`navigator.languages` 를 봅니다. IP·국가 조회는 쓰지 않습니다. 서버가 필요하고, 외부 API에 방문자 IP를 넘기게 되고, 무엇보다 "어디 있는지"와 "무엇을 읽고 싶은지"는 다릅니다.

- 선호 언어에 한국어가 있으면 **한국어**, 그 밖의 모든 경우는 **영어**
- 마크업은 한국어가 원본이고, 영어일 때만 `data-en` / `data-en-html` / `data-en-label` / `data-en-ph` 값으로 바꿉니다
- 헤더의 `KO`/`EN` 버튼으로 언제든 덮어쓸 수 있고 `localStorage` 에 남습니다. 자동 판단은 기본값일 뿐입니다
- 전환은 `location.reload()` 로 처리합니다. JS가 만들어 둔 문구(터미널 등)까지 한 번에 맞추는 가장 확실한 방법입니다
- 기계 번역 위젯은 쓰지 않습니다. 고유명사와 뉘앙스가 무너집니다

### 레이아웃이 흔들리지 않게

내용이 늘거나 줄는 구역(아코디언, 필터되는 목록)에는 두 가지를 겁니다.

1. `html { overflow-anchor: none }` — 브라우저가 스크롤을 몰래 보정하면서 화면이 밀리고 고정 헤더가 튀어나옵니다
2. 그 섹션은 `align-content: start` — 중앙 정렬이면 높이가 바뀔 때마다 블록 전체가 자리를 다시 잡습니다
3. 필터로 줄어드는 목록은 처음 높이를 `min-height` 로 잠가 둡니다

### 이스터에그

사이트마다 하나쯤 숨겨 둡니다. 단, 되돌릴 수 없는 상태 변화는 만들지 않습니다.
지금은 터미널의 숨은 명령어(`mach`)와 코나미 코드가 같은 것을 부릅니다. 화면을 가로지르는 속도선이 지나가고 그동안만 강조색이 뜨거운 색으로 바뀌었다가 2.3초 뒤 스스로 정리됩니다.
`help` 는 존재만 알리고 이름은 알려주지 않습니다.

### 고정 요소(헤더 · 점 내비)

어떤 서피스에도 속하지 않는 고정 요소는 색 토큰이 비어 브라우저 기본색으로 떨어집니다. 다크 모드에서는 그게 흰색이라 밝은 배경에서 사라집니다.
**고정 요소에는 `data-surface` 를 붙이고 JS로 뒤에 있는 섹션을 따라가게 합니다.** 배경은 `background: transparent !important` 로 지웁니다.

---

## 7. 마크업 약속

| 약속 | 뜻 |
| --- | --- |
| `data-surface="1 \| 2"` | 서피스 단계. 섹션마다 번갈아 |
| `data-section="라벨"` + `id` | 섹션 인덱스·점 내비·터미널 `sections` 가 이걸 읽습니다 |
| `data-reveal` | 등장 애니메이션 |
| `data-split="words"` | 단어 단위 마스크 등장 |
| `data-split="chars"` | 글자 단위 마스크 등장 (영어 제목용) |
| `data-nosplit` | 위 분해에서 제외 (따로 애니메이션이 붙는 요소) |
| `data-flow` | 스크롤 속도에 따라 기우는 덩어리 |
| `data-jackpot="텍스트"` | 글자가 굴러가다 멈추는 애니메이션 |
| `data-magnet` | 포인터를 살짝 따라가는 요소 |
| `data-cursor-label="라벨"` | 커서 옆에 뜨는 글자 |
| `data-cursor-box` | 커서 테두리가 흡착할 대상 |
| `data-parallax="0.1"` | 스크롤 시차 |
| `data-til="YYYY-MM-DD"` | TIL 항목. 잔디와 숫자가 여기서 만들어집니다 |
| `data-split-dir="x"` | 단어·글자가 왼쪽에서 들어오게 (기본은 아래에서) |
| `data-cursor-box` | 커서 테두리가 흡착할 대상. **테두리와 radius 가 있는 요소에만** 붙이세요. 프레임이 대상의 bounding box 와 radius 를 그대로 복사하므로, 맞을 형태가 없으면 어긋나 보입니다 |
| `<script type="application/json" id="destinations">` | 링크 목록의 단일 출처. 터미널 `ls`/`open` 과 빠른 이동이 같이 씁니다 |

---

## 8. 새 사이트를 시작할 때

1. `theme.css` 를 복사하지 말고 **같은 파일을 가리키게** 합니다. (같은 도메인이면 경로 하나, 서브도메인이면 `https://m4ch77.com/theme.css`)
2. `<html data-theme="dark">` 와 테마 저장 스크립트(`localStorage`)를 그대로 가져갑니다. 키는 `m4ch77-theme` 로 통일합니다.
3. 섹션마다 `data-surface` 를 번갈아 붙이고 `data-section` + `id` 를 답니다.
4. 헤더 · 푸터 · 빠른 이동 · 커서는 이 사이트에서 그대로 옮깁니다. 사이트마다 새로 만들지 않습니다.
5. 링크 목록은 `destinations` JSON 만 고칩니다.
6. 새 색이 필요하면 먼저 `theme.css` 에 토큰으로 추가하고, 그다음 씁니다.

---

## 9. 검토 체크리스트

- [ ] 다크 모드에서 흰 면이 없는가
- [ ] 강조색이 6절의 목록 밖에서 쓰이지 않았는가
- [ ] 폰트가 IBM Plex 두 가족뿐인가
- [ ] 모든 섹션이 한 화면에 들어가는가 (900 / 1080 높이에서 확인)
- [ ] 바로가기를 눌렀을 때 섹션이 화면 위에 정확히 붙는가 (`scroll-padding-top: 0`)
- [ ] 고정 요소가 밝은 섹션 위에서도 보이는가
- [ ] 버튼 호버에서 테두리가 겹쳐 보이지 않는가
- [ ] `prefers-reduced-motion` 에서 모션이 꺼지는가
- [ ] 키보드만으로 모든 것을 쓸 수 있는가 (Tab · 방향키 · Esc)
- [ ] 가로 스크롤이 생기지 않았는가 (`documentElement.scrollWidth === clientWidth`)

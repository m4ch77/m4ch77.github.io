/* ============================================================
   사이트 설정 — 사람이 손으로 고치는 값만 모아 둡니다
   ============================================================ */

/* 조회수 Worker 주소.

   views/README.md 의 명령을 실행하면 마지막에 이런 주소가 나옵니다.
     https://m4ch77-views.<계정>.workers.dev

   그 값을 여기 붙여 넣으면 조회수 표시와 조회수 순 정렬이 켜집니다.
   **비워 두면 기능이 조용히 꺼집니다.** 주소가 없는데 요청을 보내
   콘솔에 오류를 쌓는 것보다, 아무 일도 없는 편이 낫습니다.

   끝에 슬래시를 붙이지 마세요. */
export const VIEWS_ENDPOINT = "";
